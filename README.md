# Talha Social (React)

This is a minimal React app converted from simple static pages. It contains a login screen and a welcome page with feedback storage.

To run locally:

1. Install dependencies:

   npm install

2. Start the dev server:

   npm run dev

Login credentials for demo: username `talha`, password `123`.
